- job-template:
    name: '{name}-{pyver}'
    builders:
      - shell: 'git co {branch_name}'

- project:
   name: project-name
   pyver:
    - 27:
       branch_name: new_branch
   jobs:
    - '{name}-{pyver}'
